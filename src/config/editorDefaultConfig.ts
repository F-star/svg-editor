const config = {
  tool: 'select',

  fill: '#fff',
  stroke: '#000',
  strokeWidth: '1px',

  selectAreaFill: 'rgba(200, 200, 200, .2)',
  selectAreaStroke: '#888',
} as const

export default config
