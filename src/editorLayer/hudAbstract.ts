/**
 * elements outline
 * 对多个元素进行轮廓线高亮
 */

export abstract class Hud {
  constructor(el: Node) {
    // this.container = document.createElementNS(NS.SVG, 'g') as SVGGElement
  }
  draw() {
    //
  }
}
