/**
 * guide line layer
 */

const { NS } = require("../constants");

class GuideLineLayer {
  constructor() {
    this.container = document.createElement(NS.SVG, 'svg')
  }

  drawRect(x, y, w, h) {

  }
  clear() {

  } 
}